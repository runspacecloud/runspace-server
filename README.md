# RunSpace Server

Backend server for RunSpace.cloud.

This repository contains the RunSpace API server, authentication system, encrypted messaging backend, account recovery, sessions, billing endpoints, server APIs, and backend documentation.

Frontend clients live in separate repositories.
